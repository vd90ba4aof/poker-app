# 第三轮深度代码体检报告

> **项目**: PokerHelper Android App  
> **审查范围**: 15个Kotlin源文件 (10,345行)  
> **审查重点**: 双路识别并发竞态、Native/WebView异步时序、蓝牙状态机卡死、底层服务防御性兜底  
> **排除**: 第一轮已修复20个Bug + 第二轮已修复8个Bug (P1-NEW-1~P3-NEW-8)

---

## 总览

| 优先级 | 数量 | 说明 |
|--------|------|------|
| P0 致命 | 1 | 蓝牙命令队列永久死锁 |
| P1 严重 | 3 | 并发竞态/WebView泄漏/BLE状态不一致 |
| P2 中等 | 4 | 回调覆盖/Handler泄漏/非原子操作/重连计数器 |
| P3 低 | 2 | errorLogs并发微溢出/JS bridge方法不可达 |

---

## P0-R3-1：ESP32 BLE命令队列永久死锁

**优先级**: P0 致命  
**文件**: `Esp32BleManager.kt` — `onConnectionStateChange` + `writeCommand`  
**问题描述**:

当BLE连接断开时（GATT回调 `STATE_DISCONNECTED`），`isWriting` 标志位没有被重置为 `false`。如果断连时恰好有一个命令正在写入（`isWriting = true`），后续所有命令将永远被阻塞在队列中：

```kotlin
// Esp32BleManager.kt - gattCallback.onConnectionStateChange
BluetoothProfile.STATE_DISCONNECTED -> {
    isConnected = false
    stopHeartbeatMonitor()
    notifyStatus(false, "已断开")
    scheduleReconnect()
    // ❌ 缺失: isWriting未重置, commandQueue未清理
}

// writeCommand - 死锁路径
private fun writeCommand(cmd: String) {
    val success = bluetoothGatt?.writeCharacteristic(characteristic) ?: false
    if (success) {
        isWriting = true  // 设为true，等待onCharacteristicWrite回调
        // 但设备已断开 → onCharacteristicWrite永远不触发 → isWriting永远为true
    }
}

// sendCommand - 所有后续命令被阻塞
private fun sendCommand(cmd: String) {
    if (isWriting) {  // 永远为true
        commandQueue.add(cmd)  // 只入队不消费
        return
    }
    writeCommand(cmd)  // 永远不会执行到这里
}
```

**根因分析**:

`writeCommand` 设置 `isWriting = true` 后，依赖 `onCharacteristicWrite` 回调来调用 `processNextCommand()` 重置 `isWriting = false`。但在BLE断连场景下，如果有一个write正在进行中，GATT回调不会再触发。即使重连成功，`isWriting` 仍然为 `true`，新的write不会执行。

**修复方案**:

```diff
 // Esp32BleManager.kt - gattCallback.onConnectionStateChange
 BluetoothProfile.STATE_DISCONNECTED -> {
     Log.i(TAG, "Disconnected from GATT server, stopping heartbeat monitor")
     isConnected = false
+    // P0-R3-1: 断连时重置写入状态，防止命令队列死锁
+    isWriting = false
+    commandQueue.clear()
     stopHeartbeatMonitor()
     notifyStatus(false, "已断开")
     scheduleReconnect()
 }
```

**验证步骤**:
1. 连接ESP32后正在发送tap命令时物理断开ESP32电源
2. 等待自动重连成功
3. 再次发送tap命令，验证命令正常执行而非永久阻塞

---

## P1-R3-2：后台API HUD线程与本地CV主通道并发写VisionApiClient共享状态

**优先级**: P1 严重  
**文件**: `FloatingService.kt:2550-2600`（后台API补充线程）  
**问题描述**:

当本地CV识别成功后，会启动后台线程调用 `VisionApiClient.analyzeScreenshot(screenshot)` 用于HUD数据补充。但 `analyzeScreenshot` 是一个修改单例对象多个共享变量的方法：

```kotlin
// FloatingService.kt - 本地CV成功后的后台线程
Thread {
    try {
        // ❌ analyzeScreenshot修改以下共享@Volatile变量:
        // - VisionApiClient.lastResult
        // - VisionApiClient.holeCardsLocked
        // - VisionApiClient.streetLocked
        // - VisionApiClient.dButtonLocked
        // - VisionApiClient.dButtonPosition
        val bgResult = VisionApiClient.analyzeScreenshot(screenshot)
        // 仅使用oppHud和showdownCards...
    }
}.start()

// 与此同时，下一轮截屏可能在主线程:
// 1. 检查 holeCardsLocked (本地CV重置逻辑)
// 2. 检查 lastResult (applyDButtonInsurance中)
// 3. 依赖 streetLocked (applyStreetCorrection中)
```

在 `analyzeScreenshot` 内部，存在非原子的读-改-写序列：
```kotlin
// VisionApiClient.kt - analyzeScreenshot 内部
holeCardsLocked = result.holeCards  // 写
// ... 多个操作后 ...
lastResult = corrected  // 写
```

**根因分析**:

`@Volatile` 保证单个变量的可见性，但不保证多个变量之间的原子性。后台线程在修改 `holeCardsLocked` 和 `lastResult` 之间，主线程可能读到不一致的中间状态。例如：后台线程刚更新了 `holeCardsLocked` 但还没更新 `lastResult`，此时主线程的下一帧 `applyDButtonInsurance` 读到了新 `holeCardsLocked` 和旧 `lastResult`，导致D按钮保险逻辑判断失误。

**修复方案**:

为后台HUD补充创建一个不修改共享状态的只读分析方法：

```diff
+// VisionApiClient.kt - 新增只读分析方法，不修改任何共享状态
+fun analyzeScreenshotReadOnly(jpegData: ByteArray): VisionResult? {
+    if (apiKey.isEmpty()) return null
+    return try {
+        val compressedJpeg = compressImage(jpegData, maxWidth = 960)
+        val base64Image = Base64.encodeToString(compressedJpeg, Base64.NO_WRAP)
+        val dataUri = "data:image/jpeg;base64,$base64Image"
+        val requestJson = buildRequest(dataUri, compact = true)
+        val rawResponse = sendRequest(requestJson)
+        val result = parseResponse(rawResponse) ?: return null
+        // 不调用 applyDButtonInsurance/applyStreetCorrection/applyValidationCorrections
+        // 不修改 lastResult/holeCardsLocked/streetLocked/dButtonLocked 等共享状态
+        result
+    } catch (e: Exception) {
+        Log.w(TAG, "ReadOnly分析失败: ${e.message}")
+        null
+    }
+}

 // FloatingService.kt - 后台线程改用只读方法
 Thread {
     try {
-        val bgResult = VisionApiClient.analyzeScreenshot(screenshot)
+        val bgResult = VisionApiClient.analyzeScreenshotReadOnly(screenshot)
         if (bgResult != null && bgResult.isPokerTable) {
             // ...
```

**验证步骤**:
1. 开启自动模式，本地CV成功路径运行
2. 观察Logcat中 `holeCardsLocked` 和 `lastResult` 的变化是否只在主线程发生
3. 确认HUD数据仍能正常补充（后台线程只读取不写入）

---

## P1-R3-3：WebView reinitializeComponents未销毁旧WebView导致内存泄漏+双重回调

**优先级**: P1 严重  
**文件**: `FloatingService.kt` — `reinitializeComponents()` + `showFloatingWindow()`  
**问题描述**:

当 `FloatingService` 被系统杀死后重启（`START_STICKY` → `onStartCommand` → `reinitializeComponents`），如果 `floatingView == null`，会调用 `showFloatingWindow()` 创建全新的 WebView：

```kotlin
// FloatingService.kt - onStartCommand
if (floatingView == null || floatingBall == null) {
    showFloatingWindow()  // 创建新WebView
    showFloatingBall()
    reinitializeComponents()  // 重新注册组件...
}

// showFloatingWindow() 内部
val wv = WebView(this)  // ❌ 用Service Context创建WebView
webView = wv  // 覆盖旧引用
// ... 注册JavascriptInterface ...
wv.loadUrl(...)
```

问题：
1. **旧WebView未销毁**: 如果 `webView` 引用已存在（只是 `floatingView == null` 但 `webView != null`），直接覆盖旧引用会导致旧WebView泄漏
2. **Service Context泄漏**: WebView持有Service的Context引用，WebView的生命周期可能超过Service
3. **旧JavascriptInterface残留**: 如果旧WebView仍在运行，其JavascriptInterface中的回调（如 `autoDecision`）仍然可以触发handler.post，操作已经不存在的新UI

**根因分析**:

`reinitializeComponents` 方法没有检查并销毁已存在的WebView实例。在 `START_STICKY` 重启路径中，`floatingView` 可能因为WindowManager的Window泄漏检测被清除（导致 `floatingView == null`），但 `webView` 引用可能仍然存在。

**修复方案**:

```diff
 // FloatingService.kt - reinitializeComponents 方法开头
 private fun reinitializeComponents() {
+    // P1-R3-3: 先销毁旧WebView，防止内存泄漏和双重回调
+    try {
+        webView?.let { oldWv ->
+            oldWv.stopLoading()
+            oldWv.removeJavascriptInterface("AndroidBridge")
+            oldWv.webViewClient = object : WebViewClient() {} // 清除回调
+            oldWv.destroy()
+        }
+    } catch (_: Exception) {}
+    webView = null
+    webViewReady = false
+    pendingJsCalls.clear()
+    
     // P1-fix: 先反注册再重新注册...
     try { unregisterReceiver(notificationReceiver) } catch (_: Exception) {}
```

**验证步骤**:
1. 开启App后通过 `adb shell am kill com.pokerhelper.app` 模拟系统杀进程
2. 等待 `START_STICKY` 自动重启
3. 检查Logcat无 `Javascript error` 或 `WebView is not attached` 异常
4. 通过 `adb shell dumpsys meminfo com.pokerhelper.app` 验证无WebView泄漏

---

## P1-R3-4：BLE重连成功后计数器未重置导致重连能力逐步丧失

**优先级**: P1 严重  
**文件**: `Esp32BleManager.kt` — `scheduleReconnect()` + `gattCallback.onConnectionStateChange`  
**问题描述**:

BLE重连使用递增计数器 `reconnectAttempts`，上限为 `MAX_RECONNECT_ATTEMPTS = 5`。但在连接成功回调 `onConnectionStateChange(STATE_CONNECTED)` 中，虽然重置了 `reconnectAttempts = 0`，但如果连接成功后**很快又断开**（例如信号弱，连接持续不到1秒），`scheduleReconnect()` 会被再次调用，此时 `reconnectAttempts` 从0重新递增。

关键问题在于：每次「连接→快速断开→重连」循环消耗1次重连配额。经过5次这样的循环后，`reconnectAttempts > MAX_RECONNECT_ATTEMPTS`，**永久停止自动重连**。

```kotlin
// Esp32BleManager.kt
override fun onConnectionStateChange(gatt: BluetoothGatt, status: Int, newState: Int) {
    when (newState) {
        BluetoothProfile.STATE_CONNECTED -> {
            lastConnectedDevice = gatt.device
            reconnectAttempts = 0  // ✅ 重置
            // ...
        }
        BluetoothProfile.STATE_DISCONNECTED -> {
            // ...
            scheduleReconnect()  // 重连计数器+1
        }
    }
}

// scheduleReconnect
if (reconnectAttempts > MAX_RECONNECT_ATTEMPTS) {  // 5次后永久停止
    Log.w(TAG, "★ BLE重连次数超限")
    return  // ❌ 永久停止，即使ESP32重新上线也无法恢复
}
```

**根因分析**:

设计意图是防止无限重连浪费电量，但实际场景中ESP32可能因为暂时信号干扰反复断连-重连。5次快速断连后，即使ESP32恢复正常距离，App也永远不会再尝试连接，直到用户手动重启App。

**修复方案**:

引入重连冷却机制：成功连接后持续一段时间（如30秒）才视为"稳定连接"，只有不稳定连接才算消耗配额；稳定连接后断连时完全重置计数器：

```diff
+private var lastStableConnectTime = 0L
+private val STABLE_CONNECTION_THRESHOLD = 30000L  // 连接持续30秒视为稳定

 override fun onConnectionStateChange(gatt: BluetoothGatt, status: Int, newState: Int) {
     when (newState) {
         BluetoothProfile.STATE_CONNECTED -> {
             lastConnectedDevice = gatt.device
-            reconnectAttempts = 0
+            // P1-R3-4: 记录连接时间，断开时判断是否为稳定连接
+            lastStableConnectTime = System.currentTimeMillis()
+            reconnectAttempts = 0
             handler.post { gatt.requestMtu(512) }
         }
         BluetoothProfile.STATE_DISCONNECTED -> {
+            // P1-R3-4: 如果连接持续超过30秒，视为稳定连接后的正常断连，完全重置
+            val connectedDuration = System.currentTimeMillis() - lastStableConnectTime
+            if (connectedDuration > STABLE_CONNECTION_THRESHOLD) {
+                reconnectAttempts = 0
+                Log.i(TAG, "稳定连接(${connectedDuration/1000}s)后断连，重置重连计数器")
+            }
             isConnected = false
             stopHeartbeatMonitor()
             notifyStatus(false, "已断开")
             scheduleReconnect()
         }
     }
 }
```

**验证步骤**:
1. 连接ESP32后手动关闭ESP32电源，等待重连
2. 重复「开启→关闭ESP32」10次以上
3. 验证每次ESP32开启后App都能自动重连
4. 检查Logcat中 `reconnectAttempts` 在稳定连接后确实被重置

---

## P2-R3-5：ScreenOptService.onScreenshotReady 三路径覆盖竞态

**优先级**: P2 中等  
**文件**: `FloatingService.kt` — `triggerCapture()` + `autoCaptureTrigger()` + `triggerMultiFrameCapture()`  
**文件**: `HttpServerService.kt` — `/api/capture`  
**问题描述**:

`ScreenOptService.onScreenshotReady` 是一个单回调引用，被4个不同的代码路径写入：

| 路径 | 线程 | 写入位置 |
|------|------|----------|
| `triggerCapture()` | 主线程 | 手动截屏 |
| `autoCaptureTrigger()` | 主线程(handler.post) | 自动截屏 |
| `triggerMultiFrameCapture()` | 主线程(handler.post) | 多帧截屏 |
| `/api/capture` | NanoHTTPD工作线程 | HTTP API |

虽然 `triggerCapture()` 有 `isVisionInProgress` 保护防止与自动截屏冲突，但 `/api/capture` 在**NanoHTTPD工作线程**中执行：

```kotlin
// HttpServerService.kt - /api/capture (NanoHTTPD工作线程)
if (ScreenOptService.onScreenshotReady != null) {
    // 返回"capture_in_progress"
} else if (ScreenOptService.isServiceRunning()) {
    val latch = CountDownLatch(1)
    ScreenOptService.onScreenshotReady = { success ->  // ❌ 覆写主线程的回调
        latch.countDown()
    }
    ScreenOptService.captureScreen()
    latch.await(3, TimeUnit.SECONDS)
}
```

**竞态窗口**: 主线程的 `autoCaptureTrigger()` 刚设置完 `onScreenshotReady`，还没来得及调用 `captureScreen()`，此时 NanoHTTPD 线程检查 `onScreenshotReady != null` 为 true → 返回 "capture_in_progress"。或者反过来：NanoHTTPD 线程先检查 `onScreenshotReady == null`，然后主线程设置了回调，NanoHTTPD 线程覆写了主线程的回调。

**根因分析**:

`onScreenshotReady` 的读-判断-写不是原子操作。4个路径使用同一个回调槽位，即使每个路径内部有保护，跨路径之间仍无互斥。

**修复方案**:

为HTTP API路径增加独立的截屏方法，不依赖共享回调：

```diff
 // ScreenOptService.kt - 新增同步截屏方法
+/**
+ * P2-R3-5: 同步截屏方法，不依赖共享回调
+ * 供HttpServerService等外部调用，避免回调覆盖
+ */
+fun captureScreenSync(timeoutMs: Long = 3000): Boolean {
+    val svc = instance ?: return false
+    val latch = CountDownLatch(1)
+    var result = false
+    val originalCallback = onScreenshotReady
+    onScreenshotReady = { success ->
+        result = success
+        latch.countDown()
+    }
+    svc.performCapture()
+    latch.await(timeoutMs, TimeUnit.MILLISECONDS)
+    // 恢复原始回调
+    onScreenshotReady = originalCallback
+    return result
+}

 // HttpServerService.kt - /api/capture
- val latch = java.util.concurrent.CountDownLatch(1)
- var captureSuccess = false
- ScreenOptService.onScreenshotReady = { success ->
-     captureSuccess = success
-     latch.countDown()
- }
- ScreenOptService.captureScreen()
- latch.await(3, java.util.concurrent.TimeUnit.SECONDS)
+ val captureSuccess = ScreenOptService.captureScreenSync(3000)
```

**验证步骤**:
1. 同时启动自动截屏模式和HTTP API截屏
2. 验证两者不会互相覆盖回调
3. 检查截屏结果不出现"回调丢失"或"截屏空白"

---

## P2-R3-6：FloatingService.onDestroy未清理ballSignalHandler

**优先级**: P2 中等  
**文件**: `FloatingService.kt` — `onDestroy()` + `startBallSignal()`  
**问题描述**:

```kotlin
// FloatingService.kt - startBallSignal() 创建独立Handler
private var ballSignalRunnable: Runnable? = null
private var ballSignalHandler: android.os.Handler? = null

private fun startBallSignal(freqHz: Int) {
    if (ballSignalHandler == null) 
        ballSignalHandler = android.os.Handler(android.os.Looper.getMainLooper())
    // ... 创建Runnable并postDelayed
}

// onDestroy - ❌ 未清理ballSignalHandler
override fun onDestroy() {
    handler.removeCallbacksAndMessages(null)  // 只清理了主handler
    // ballSignalHandler的runnable仍在消息队列中
    // floatingBall已被removeView，但ballSignalRunnable仍持有floatingBall引用
}
```

**根因分析**:

`ballSignalHandler` 是一个独立的Handler实例，其消息队列独立于主 `handler`。Service销毁后，闪烁Runnable仍会在主线程执行，尝试操作已被removeView的 `floatingBall`。虽然try-catch防止了崩溃，但会造成不必要的CPU唤醒和资源浪费。

**修复方案**:

```diff
 // FloatingService.kt - onDestroy()
 override fun onDestroy() {
     isRunning = false
     try { ScreenOptService.onScreenshotReady = null } catch (_: Exception) {}
     try { wakeLock?.let { if (it.isHeld) it.release() } } catch (_: Exception) {}
     wakeLock = null
     currentPanelWidth = 0
     currentPanelHeight = 0
     handler.removeCallbacksAndMessages(null)
+    // P2-R3-6: 清理闪烁信号Handler
+    ballSignalRunnable?.let { ballSignalHandler?.removeCallbacks(it) }
+    ballSignalRunnable = null
+    ballSignalHandler = null
     speechRecognizer?.destroy()
```

**验证步骤**:
1. 触发悬浮球闪烁信号（如检测不到牌桌）
2. 立即停止Service
3. 验证 `adb shell dumpsys activity service com.pokerhelper.app.FloatingService` 无泄漏Handler

---

## P2-R3-7：HttpServerService /api/hotload在NanoHTTPD工作线程执行阻塞网络I/O

**优先级**: P2 中等  
**文件**: `HttpServerService.kt` — `/api/hotload` 路由  
**问题描述**:

NanoHTTPD默认使用有限的工作线程池处理HTTP请求。`/api/hotload` 路由直接在 `serve()` 方法中执行同步网络I/O：

```kotlin
// HttpServerService.kt - /api/hotload
session.uri == "/api/hotload" -> {
    // ❌ 同步阻塞网络I/O，最长15秒
    val conn = URL(HOTLOAD_URL).openConnection()
    conn.connectTimeout = HOTLOAD_TIMEOUT / 2  // 7.5秒
    conn.readTimeout = HOTLOAD_TIMEOUT / 2
    html = conn.getInputStream().bufferedReader(Charsets.UTF_8).readText()
    
    // 失败还有第二个URL，又是7.5秒
    if (html == null || html.isEmpty()) {
        val conn2 = URL(HOTLOAD_URL_FALLBACK).openConnection()
        conn2.connectTimeout = HOTLOAD_TIMEOUT  // 15秒
        html = conn2.getInputStream().bufferedReader(Charsets.UTF_8).readText()
    }
}
```

最坏情况下（两个URL都超时），一个HTTP请求会阻塞工作线程长达 **22.5秒**。如果此时有其他请求（如 `/api/status` 健康检查），会被排队等待。

**根因分析**:

NanoHTTPD的 `serve()` 方法在调用线程上同步执行。虽然NanoHTTPD使用线程池（默认线程数 = CPU核心数），但长时间阻塞会耗尽线程池。

**修复方案**:

```diff
 // HttpServerService.kt - /api/hotload
 session.uri == "/api/hotload" -> {
+    // P2-R3-7: 异步热更新，立即返回"已触发"响应
+    Thread({
+        try {
+            var html: String? = null
+            // ... 原有下载逻辑 ...
+            if (html != null && html.isNotEmpty() && html.contains("poker") && html.length > 1000) {
+                pokerHelperHtml = html
+                hotloadSource = "remote"
+                File(filesDir, HOTLOAD_FILE).writeText(html, Charsets.UTF_8)
+            }
+        } catch (_: Exception) {}
+    }, "HotloadThread").start()
+    val json = JSONObject().apply {
+        put("ok", true)
+        put("status", "downloading")
+    }.toString()
+    newFixedLengthResponse(Response.Status.OK, "application/json", json).apply {
+        addHeader("Access-Control-Allow-Origin", "*")
+    }
-    // 原同步下载代码...
 }
```

**验证步骤**:
1. 模拟网络慢环境（`adb forward tcp:8666 tcp:8666` + iptables限速）
2. 触发 `/api/hotload`
3. 同时请求 `/api/status`，验证不被阻塞

---

## P2-R3-8：FloatingService.errorLogs add/removeAt 非原子操作

**优先级**: P2 中等  
**文件**: `FloatingService.kt` — `addErrorLog()`  
**问题描述**:

```kotlin
// FloatingService.kt
private val errorLogs = java.util.Collections.synchronizedList(mutableListOf<String>())
private val MAX_ERROR_LOGS = 50

private fun addErrorLog(entry: String) {
    errorLogs.add(entry)
    if (errorLogs.size > MAX_ERROR_LOGS) errorLogs.removeAt(0)  // ❌ 非原子
    // ...
}
```

`errorLogs` 使用 `synchronizedList` 包装，每个单独的 `add()`、`size`、`removeAt()` 调用都是线程安全的。但 `add` → 检查 `size` → `removeAt` 这三步操作合起来不是原子的。

并发场景：`@JavascriptInterface` 后台线程（如 `getErrorLogs()` 调用 `joinToString`）和主线程（`addErrorLog`）同时操作，可能导致：
- 临时超过 MAX_ERROR_LOGS
- `removeAt(0)` 时 list 为空抛出 `IndexOutOfBoundsException`（被catch吞掉）

**根因分析**:

`synchronizedList` 只保证单个方法的原子性，复合操作需要外部同步块。

**修复方案**:

```diff
 private fun addErrorLog(entry: String) {
-    errorLogs.add(entry)
-    if (errorLogs.size > MAX_ERROR_LOGS) errorLogs.removeAt(0)
+    // P2-R3-8: 使用同步块保证复合操作原子性
+    synchronized(errorLogs) {
+        errorLogs.add(entry)
+        if (errorLogs.size > MAX_ERROR_LOGS) errorLogs.removeAt(0)
+    }
     try {
         File(filesDir, ERROR_LOG_FILE).appendText(entry + "\n", Charsets.UTF_8)
         val file = File(filesDir, ERROR_LOG_FILE)
         if (file.length() > 32768) {
             val lines = file.readLines()
             file.writeText(lines.takeLast(MAX_ERROR_LOGS).joinToString("\n") + "\n")
         }
     } catch (_: Exception) {}
 }
```

**验证步骤**:
1. 从JS端高频调用 `AndroidBridge.getErrorLogs()` 同时主线程频繁 `addErrorLog`
2. 验证 `errorLogs.size` 始终 ≤ MAX_ERROR_LOGS + 1
3. 无 `IndexOutOfBoundsException` 异常日志

---

## P3-R3-9：setBlinkFreq方法在FloatingService上而非匿名JS接口对象

**优先级**: P3 低  
**文件**: `FloatingService.kt` — `setBlinkFreq()`  
**问题描述**:

```kotlin
// FloatingService.kt - setBlinkFreq 定义在FloatingService类上
// V2.9.131: 公开 setBlinkFreq 供 JS 端直接调用
@JavascriptInterface
fun setBlinkFreq(freq: Int) {
    handler.post {
        try { startBallSignal(freq) } catch (_: Exception) {}
    }
}

// 但 addJavascriptInterface 绑定的是匿名内部类对象
wv.addJavascriptInterface(object : Any() {
    @JavascriptInterface
    fun updateStatus(text: String) { ... }
    @JavascriptInterface
    fun showAdvice(advice: String) { ... }
    // ❌ setBlinkFreq 不在这个对象中
}, "AndroidBridge")
```

JS端调用 `AndroidBridge.setBlinkFreq(3)` 会因为方法不存在而静默失败。`@JavascriptInterface` 注解只在被 `addJavascriptInterface` 绑定的对象上生效。

**根因分析**:

方法放在了错误的类上。`@JavascriptInterface` 注解的方法必须在传给 `addJavascriptInterface` 的对象中定义。

**修复方案**:

```diff
 // FloatingService.kt - 在匿名JS接口对象中添加
 wv.addJavascriptInterface(object : Any() {
     // ... 已有的接口方法 ...
+    
+    @JavascriptInterface
+    fun setBlinkFreq(freq: Int) {
+        handler.post {
+            try { startBallSignal(freq) } catch (_: Exception) {}
+        }
+    }
 }, "AndroidBridge")

 // 可选：删除FloatingService上的旧方法
-@JavascriptInterface
-fun setBlinkFreq(freq: Int) { ... }
```

**验证步骤**:
1. JS端执行 `AndroidBridge.setBlinkFreq(3)`
2. 验证悬浮球开始3Hz快闪
3. 执行 `AndroidBridge.setBlinkFreq(0)` 验证停止闪烁

---

## P3-R3-10：DiagnosticLogger.calcChipDelta/calcPotDelta 缺少实现上下文

**优先级**: P3 低  
**文件**: `DiagnosticLogger.kt` — `logRecognition()` 内部调用  
**问题描述**:

`DiagnosticLogger.logRecognition()` 中调用了 `calcChipDelta()` 和 `calcPotDelta()` 方法。这两个方法的实现依赖于对前一次识别结果的缓存比较：

```kotlin
// DiagnosticLogger.kt - logRecognition内部
val chipDelta = vlmResult?.let { calcChipDelta(it.playerChips) }
val chipStatus = determineChipStatus(chipDelta)
val potDelta = vlmResult?.let { calcPotDelta(it.potSize) }
```

但 `DiagnosticLogger` 是一个全局单例 `object`，在多线程环境下（主线程的 `processScreenshotAndAnalyze` + 后台HUD线程），`calcChipDelta` 和 `calcPotDelta` 内部的状态变量如果没有同步保护，可能产生不一致的delta值。

**根因分析**:

`logRecognition` 本身被两个不同线程调用（主线程的识别结果记录 + 后台线程的API补充），而 `DiagnosticLogger` 的状态追踪（如上一次筹码值）没有线程同步。

**修复方案**:

为 `DiagnosticLogger` 的状态追踪变量添加 `@Volatile` 或同步保护：

```diff
+@Volatile
+private var lastPlayerChips = 0
+@Volatile
+private var lastPotSize = 0

 private fun calcChipDelta(currentChips: Int): Long {
-    val delta = currentChips - lastPlayerChips
-    lastPlayerChips = currentChips
-    return delta.toLong()
+    return synchronized(this) {
+        val delta = currentChips - lastPlayerChips
+        lastPlayerChips = currentChips
+        delta.toLong()
+    }
 }
```

**验证步骤**:
1. 导出诊断日志，检查 `chipDelta` 和 `potDelta` 值合理性
2. 验证后台HUD线程不影响主线程的delta计算

---

## 附录：方向覆盖矩阵

| 方向 | 发现的问题 | Bug编号 |
|------|-----------|---------|
| **方向1: 双路识别并发竞态** | 后台API HUD线程并发写共享状态 | P1-R3-2 |
| | errorLogs非原子操作 | P2-R3-8 |
| **方向2: Native/WebView异步时序** | WebView重初始化未销毁旧实例 | P1-R3-3 |
| | JS bridge方法绑定在错误对象上 | P3-R3-9 |
| **方向3: 蓝牙状态机卡死** | BLE命令队列断连死锁 | P0-R3-1 |
| | 重连计数器未因稳定连接重置 | P1-R3-4 |
| **方向4: 底层服务防御性兜底** | onScreenshotReady多路径覆盖 | P2-R3-5 |
| | ballSignalHandler未清理 | P2-R3-6 |
| | HTTP热更新阻塞工作线程 | P2-R3-7 |
| | DiagnosticLogger状态追踪无同步 | P3-R3-10 |

---

## 修复优先级建议

1. **立即修复**: P0-R3-1（BLE命令死锁 — 直接影响全自动执行）
2. **本迭代修复**: P1-R3-2（并发竞态 — 可能导致识别数据错乱）
3. **本迭代修复**: P1-R3-3（WebView泄漏 — 长时间运行后内存增长）
4. **本迭代修复**: P1-R3-4（BLE重连能力丧失 — 影响用户体验）
5. **下迭代修复**: P2-R3-5~P2-R3-8（中等优先级）
6. **择机修复**: P3-R3-9~P3-R3-10（低优先级）

---

*报告生成时间: 2025-01-XX*  
*审查工具: 手动代码审计*  
*审查人: 资深Android架构师 Subagent*
