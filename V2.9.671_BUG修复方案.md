# V2.9.670 BUG修复方案

## 一、问题概述

基于实战日志（48手牌，-11BB）和代码分析，确认5个导致负EV的BUG：

| 优先级 | BUG | 日志证据 | 影响 |
|--------|-----|----------|------|
| P0 | DRTA.hands计数器重复递增 | 48手牌显示hands=60，VPIP=145% | 对手分类错误，剥削调整失效 |
| P1 | OppProfiler.getStat()方法缺失 | 47手fish对手，0条剥削标签 | JJ不3bet，22手preflop fold |
| P1 | TableQualityMeter数据源错误 | 初始化后0条评估输出 | 换桌建议仅基于码深，无桌质评估 |
| P1 | _facingCR street变量作用域 | 代码确认（日志未触发） | turn/river面对CR用flop调整 |
| P1 | limp兜底位置限制缺失 | 3次limp全亏-5BB | UTG/MP位置limp入池后翻牌miss率高 |

---

## 二、BUG详细分析与修复

### BUG 1: DRTA.hands计数器重复递增（P0）

**日志证据**：
- 48手牌，但`drtaTracker.hands=60`
- `drtaTracker.vpip=87`，计算得VPIP=87/60=145%（不可能超过100%）

**根因**：
```javascript
// poker_helper.html line 5820
record:function(action,street,isFacingBet){
  var t=this.tracker;
  t.hands++;  // 每次调用都递增
  // ...
}
```

`record()`在preflop/flop/turn/river每个街都被调用，一手打到river会递增4次。

**影响**：
- VPIP等统计失真
- 对手分类错误（fish/tag/nit判断错误）
- 剥削调整基于错误数据

**修复**：
```javascript
record:function(action,street,isFacingBet){
  var t=this.tracker;
  // V2.9.671 FIX(P0): hands计数器去重——只在preflop街首次调用时递增
  if(street==='preflop'){
    var _handKey=(G.hole&&G.hole[0]&&G.hole[1])?(G.hole[0].rank+G.hole[0].suit+G.hole[1].rank+G.hole[1].suit):'';
    if(t._lastCountedHand!==_handKey){
      t.hands++;
      t._lastCountedHand=_handKey;
    }
  }
  // ...
}
```

同时在`DRTA.init()`中添加`_lastCountedHand:''`初始化。

---

### BUG 2: OppProfiler.getStat()方法缺失（P1）

**日志证据**：
- 47/48手对手被分类为fish
- 所有reason都是纯GTO输出（"GTO call vs MP"、"GTO 面CBet fold"）
- **0条剥削标签**（如"过度弃牌剥削"、"跟注站不诈唬"）

**根因**：
```javascript
// poker_helper.html line 5127-5150
function _facingCBet(k,hcKey,btKey,betSz,pot,eq){
  // ...
  if(OppProfiler&&OppProfiler.getStat){
    var oppCB=OppProfiler.getStat('cbetFlop')||0;  // 调用不存在的方法
    if(oppCB>.7){freq*=1.3;}
    // ...
  }
}
```

代码多处调用`OppProfiler.getStat('cbetFlop')`、`OppProfiler.getStat('foldToCBetFlop')`、`OppProfiler.getStat('af')`，但`getStat()`方法从未定义→返回`undefined`→所有剥削调整分支被跳过。

**影响**：
- JJ翻前对fish只call不3bet（应触发"跟注站价值加码"）→翻后miss输6BB
- 3bet范围对fish不扩大（应触发"过度弃牌剥削"）→22手preflop fold
- 面CBet策略对fish不调整→As8s翻牌fold（eq=35%>赔率25%，应call）

**修复**：
```javascript
// poker_helper.html line 1999 添加getStat()方法
getStat:function(statName){
  try{
    var sum=0,weight=0;
    for(var nk in this._profiles){
      var p=this._profiles[nk];
      if(!p)continue;
      
      if(statName==='cbetFlop'){
        // cbet频率 = cbet次数 / 作为PFR看到flop的次数
        var cbetOpp=p._sawFlopAsPFR||0;
        if(cbetOpp>=2){
          var cbetRate=(p._cbetFlop||0)/cbetOpp;
          sum+=cbetRate*cbetOpp;  // 加权
          weight+=cbetOpp;
        }
      }
      else if(statName==='foldToCBetFlop'){
        // fold-to-cbet频率
        var facedCBet=p._facedCBet||0;
        if(facedCBet>=2){
          var foldRate=(p._foldToCBet||0)/facedCBet;
          sum+=foldRate*facedCBet;
          weight+=facedCBet;
        }
      }
      else if(statName==='af'){
        // 攻击性因子 = (bet+raise) / call
        var aggActions=(p._raiseFlop||0)+(p._raiseTurn||0)+(p._raiseRiver||0);
        var passiveActions=(p._callFlop||0)+(p._callTurn||0)+(p._callRiver||0);
        if(passiveActions>=2){
          var af=aggActions/Math.max(passiveActions,1);
          sum+=af*passiveActions;
          weight+=passiveActions;
        }
      }
    }
    
    if(weight<2)return null;  // 样本不足
    return sum/weight;
  }catch(e){
    console.log('[OppProfiler] getStat异常: '+e.message);
    return null;
  }
}
```

---

### BUG 3: TableQualityMeter数据源错误（P1）

**日志证据**：
- `[TableQualityMeter] 初始化完成（2.9.670，4阈值+3级）`
- 之后再无任何TableQualityMeter评估输出
- 换桌建议只触发"起始码深止损"（独立分支）

**根因**：
```javascript
// poker_helper.html line 6770-6816
_avgVPIP:function(){
  try{
    if(typeof DRTA==='undefined'||!DRTA.players)return null;  // DRTA.players不存在
    // ...
  }
}
```

`_avgVPIP()`、`_lowWTSDCnt()`、`_threeBetRate()`都检查`DRTA.players`，但DRTA没有`players`属性（只有`tracker`对象）→返回null→桌质评估永远不触发。

**修复**：
```javascript
_avgVPIP:function(){
  try{
    if(typeof OppProfiler==='undefined'||!OppProfiler._profiles)return null;
    var sum=0,n=0;
    for(var nk in OppProfiler._profiles){
      var p=OppProfiler._profiles[nk];
      if(p&&typeof p.hands==='number'&&p.hands>=3){
        var vpip=p.hands>0?Math.round(p.entered/p.hands*100):0;
        sum+=vpip;
        n++;
      }
    }
    return n>=3?sum/n:null;
  }catch(e){return null;}
}

_lowWTSDCnt:function(){
  try{
    if(typeof OppProfiler==='undefined'||!OppProfiler._profiles)return null;
    var cnt=0,total=0;
    for(var nk in OppProfiler._profiles){
      var p=OppProfiler._profiles[nk];
      if(p&&typeof p.hands==='number'&&p.hands>=5){
        total++;
        var wtsd=p._sawFlop>0?Math.round((p._sawShowdown||0)/p._sawFlop*100):100;
        if(wtsd<this.THRESHOLDS.WTSD_TIGHT)cnt++;
      }
    }
    return total>=this.THRESHOLDS.WTSD_OPPS?{low:cnt,total:total}:null;
  }catch(e){return null;}
}

_threeBetRate:function(){
  try{
    if(typeof OppProfiler==='undefined'||!OppProfiler._profiles)return null;
    var sum3=0,sumOpp=0;
    for(var nk in OppProfiler._profiles){
      var p=OppProfiler._profiles[nk];
      if(p&&typeof p.hands==='number'&&p.hands>=5){
        sum3+=p._pf3bet||0;
        sumOpp+=p._pfOpenOpp||0;
      }
    }
    return sumOpp>0?(sum3/sumOpp*100):null;
  }catch(e){return null;}
}
```

---

### BUG 4: _facingCR street变量作用域（P1）

**代码证据**：
```javascript
// poker_helper.html line 4249
var _fcrR=_facingCR(k,hcKey,btKey,bet,pot,eq);  // 未传street参数

// line 5136
var _street2=(typeof street!=='undefined')?street:(typeof G!=='undefined'&&G.street?G.street:'flop');
// street变量不在函数作用域内，G.street从未赋值→_street2恒为'flop'
```

**影响**：
- turn面对CR：应adjC-0.06, adjR-0.02, adjF+0.08，实际用flop的0调整
- river面对CR：应adjC-0.10, adjR-0.03, adjF+0.13，实际用flop的0调整

日志中未触发CR场景（`scene==='reraise' && didPFR===true`），但代码缺陷确认。

**修复**：
```javascript
// line 4249: 调用时传递street参数
var _fcrR=_facingCR(k,hcKey,btKey,bet,pot,eq,street);

// line 5128: 函数签名添加street参数
function _facingCR(k,hcKey,btKey,betSz,pot,eq,street){
  // ...
  // line 5136: 直接使用传入的street
  var _street2=street||'flop';
  // ...
}
```

---

### BUG 5: limp兜底位置限制缺失（P1）

**日志证据**：
| 手牌 | 位置 | reason | 结果 |
|------|------|--------|------|
| A8s | co | limp兜底 eq62%≥需29% | 翻牌fold -2BB |
| K9s | mp | limp兜底 eq52%≥需40% | 翻牌fold -1BB |
| A6s | utg | limp兜底 eq50%≥需40% | 翻前fold -2BB |

3次limp兜底全亏-5BB。

**根因**：
```javascript
// poker_helper.html line 3730
if(_isLimp){
  // 没有位置检查
  // ...
}
```

UTG/MP位置limp入池后：
- 没有位置优势，翻牌miss后难以继续
- 容易被3bet，导致更大损失（A6s limp后被加注fold -2BB）

对比：FlatCall防御分支（line 3714）有位置调整`if(p==='utg'||p==='mp')_fcTh+=3`，但limp兜底没有。

**修复**：
```javascript
// line 3618 添加位置检查
var _isLimp=(_betBB>0&&_betBB<=2.5&&pot<=4);
// V2.9.671 FIX(P1): limp兜底位置限制——UTG/MP位置禁止limp兜底
var _limpPosOK=(p!=='utg'&&p!=='mp');  // 只允许CO/BTN/SB/BB位置limp兜底

// line 3730 添加位置条件
if(_isLimp&&_limpPosOK){
  // ...
}
```

---

## 三、修复影响评估

### 预期EV改善

| BUG | 修复前 | 修复后 | 预期改善 |
|-----|--------|--------|----------|
| Bug 1 | VPIP=145%（失真） | VPIP正常（≤100%） | 对手分类准确，剥削调整生效 |
| Bug 2 | 0条剥削标签 | 对fish触发剥削 | JJ翻前3bet vs fish，减少preflop fold |
| Bug 3 | 桌质评估失效 | 桌质评估正常 | 换桌建议基于桌质+码深 |
| Bug 4 | turn/river CR用flop调整 | 使用正确街调整 | CR决策更准确 |
| Bug 5 | UTG/MP limp全亏 | 禁止UTG/MP limp | 避免-5BB/48手 |

### 具体场景改善

**场景1：JJ翻前 vs fish**
- 修复前：GTO call vs MP(35%) → 翻后miss输6BB
- 修复后：触发"跟注站价值加码"→3bet JJ → 翻前拿下底池，避免翻后风险

**场景2：As8s翻牌 vs fish cbet**
- 修复前：面CBet fold 0/9(30%) → fold（eq=35%>赔率25%，错误）
- 修复后：触发"过度弃牌剥削"（fish fold-to-cbet高）→ call或raise

**场景3：UTG limp K9s**
- 修复前：limp兜底 → 翻牌miss fold -1BB
- 修复后：位置限制触发 → 直接fold，避免入池

---

## 四、修复清单

- [x] Bug 1: DRTA.hands计数器去重（line 5820-5832）
- [x] Bug 2: OppProfiler.getStat()方法添加（line 1999-2045）
- [x] Bug 3: TableQualityMeter数据源修正（line 6770-6816）
- [x] Bug 4: _facingCR street参数传递（line 4249, 5128-5136）
- [x] Bug 5: limp兜底位置限制（line 3618, 3730）

---

## 五、测试建议

1. **对手分类测试**：打100手，检查DRTA.tracker.hands与实际手数是否一致，VPIP是否≤100%
2. **剥削调整测试**：对fish对手，检查reason中是否出现剥削标签（"过度弃牌剥削"、"跟注站不诈唬"等）
3. **桌质评估测试**：打50手，检查TableQualityMeter是否有评估输出
4. **limp位置测试**：UTG/MP位置limp场景，检查是否直接fold而非limp兜底

---

## 六、版本信息

- 修复版本：V2.9.671
- 修复日期：2026-09-19
- 修复文件：`app/src/main/assets/poker_helper.html`
- 修复行数：5处，共约150行代码
