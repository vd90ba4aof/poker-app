name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: JS engine runtime smoke test
        # 运行时行为门禁：加载真实poker_helper.html喂vision数据，断言go()决策时刻stk/pot已就绪。
        # 拦截静态检查抓不到的时序/分支回归（如2026-09-01 stk时序倒置：决策用默认100BB）。
        run: node tests/engine_smoke.test.js

      - name: Build Debug APK
        run: ./gradlew assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: poker-app-debug
          path: app/build/outputs/apk/debug/*.apk

      - name: Read version
        id: version
        run: |
          VERSION=$(grep 'versionName' app/build.gradle | head -1 | sed 's/.*"\(.*\)".*/\1/')
          echo "VERSION=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Create Release
        if: success()
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.VERSION }}
          name: "青云 v${{ steps.version.outputs.VERSION }}"
          body: "V${{ steps.version.outputs.VERSION }}: BLE连接修复+版本同步"
          files: app/build/outputs/apk/debug/*.apk
          generate_release_notes: false
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}