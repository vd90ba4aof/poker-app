# Poker-App 第二轮深度代码体检报告

> 审查范围：15个Kotlin文件，共10319行
> 排除范围：上一轮已修复的20个Bug (P0-1~P0-4, P1-5, P1-7, P1-8, P2-11, P2-12, P2-16~P2-19)

---

### P1-NEW-1: executeExactBet在主线程Thread.sleep导致ANR
- **优先级**: P1（严重）
- **文件**: FloatingService.kt 第1003、1013、1030行
- **业务意图**: 精确金额输入——依次点击输入框→清空旧值→逐位输入数字→点确认
- **根因推理链**:
  1. `executeAutoTap` 被 `autoDecision` 回调触发，`autoDecision` 在 `@JavascriptInterface` 方法内通过 `handler.post {}` 运行在**主线程**
  2. `executeAutoTap` → `executeExactBet(amount)` 直接在主线程执行
  3. `executeExactBet` 内部有3处 `Thread.sleep`：
     - L1003: `Thread.sleep(250)` 等键盘弹出
     - L1013: `repeat(10) { Thread.sleep(40) }` 清空旧值，总计 400ms
     - L1030: `digits.forEach { Thread.sleep(60) }` 输入数字，最多6位 = 360ms
  4. 最坏情况总阻塞：250 + 400 + 360 = **1010ms**
  5. Android ANR阈值：前台服务 5秒，但连续阻塞>1秒即可触发系统 watchdog 告警，用户可感知明显卡顿
- **全局影响面**: 全自动模式下执行精确下注时，UI完全冻结约1秒，可能导致系统弹出ANR对话框终止应用。在牌局关键时刻（用户需要快速操作），ANR = 死局。
- **精简Diff**:
```diff
--- a/app/src/main/java/com/pokerhelper/app/FloatingService.kt
+++ b/app/src/main/java/com/pokerhelper/app/FloatingService.kt
@@ -990,6 +990,9 @@ class FloatingService : Service() {
     // V3.14: 精确金额输入 — 点击输入框+逐个点数字键盘 (GG数字键盘坐标需实测)
     private fun executeExactBet(amount: Int): Boolean {
+        // 安全检查：禁止在主线程sleep
+        if (Thread.currentThread() == mainLooper.thread) {
+            Log.w(TAG, "executeExactBet: 主线程调用，切换到后台线程")
+            Thread { executeExactBet(amount) }.start()
+            return true  // 异步执行，不阻塞
+        }
         try {
             Log.d(TAG, "executeExactBet: amount=$amount")
             val cfg = GameModeConfig.getCoordinateConfig()
```
- **验证清单**:
  1. 在自动模式下触发精确下注（金额非标准档），观察是否出现UI卡顿
  2. 使用StrictMode检测主线程sleep
  3. 验证修复后下注操作仍然正确执行
- **置信度**: 高

---

### P1-NEW-2: analyzeMultipleScreenshots并发修改单例共享状态
- **优先级**: P1（严重）
- **文件**: VisionApiClient.kt 第1060-1068行
- **业务意图**: 多桌并行分析——用 `async(Dispatchers.IO)` 同时分析多张截图
- **根因推理链**:
  1. `analyzeMultipleScreenshots` 使用 `coroutineScope { mapIndexed { async(Dispatchers.IO) { ... } }.awaitAll() }` 并行执行
  2. 每个 `async` 调用 `analyzeSingleScreenshotSafe` → `analyzeScreenshot(jpegData)`
  3. `analyzeScreenshot` 是 `VisionApiClient`（object单例）的方法，内部写入大量共享字段：`lastResult`、`lastResultTime`、`holeCardsLocked`、`dButtonLocked`、`streetLocked` 等
  4. 这些字段虽有 `@Volatile`，但 `analyzeScreenshot` 内的读写模式是**非原子复合操作**：先读 `holeCardsLocked`，判断后写入 `lastResult`，再读 `lastResult` 做保险计算
  5. 2个并行任务同时执行时，Task A 写入 `holeCardsLocked` 后，Task B 覆盖了它，导致 Task A 后续逻辑读到错误值
- **全局影响面**: 多桌场景下识别结果错乱——桌A的手牌可能被锁到桌B的结果上，底池/跟注金额交叉污染。用户看到完全错误的策略建议。
- **精简Diff**:
```diff
--- a/app/src/main/java/com/pokerhelper/app/VisionApiClient.kt
+++ b/app/src/main/java/com/pokerhelper/app/VisionApiClient.kt
@@ -1060,9 +1060,13 @@ object VisionApiClient {
         }
         return try {
             coroutineScope {
-                screenshotPaths.mapIndexed { index, path ->
-                    async(Dispatchers.IO) {
-                        analyzeSingleScreenshotSafe(index, path)
+                // P1-fix: 多桌分析不能并行——analyzeScreenshot修改单例共享状态
+                // 改为串行执行，避免并发污染holeCardsLocked/lastResult等字段
+                val results = mutableListOf<MultiTableResult>()
+                for ((index, path) in screenshotPaths.withIndex()) {
+                    val result = withContext(Dispatchers.IO) {
+                        analyzeSingleScreenshotSafe(index, path)
                     }
-                }.awaitAll()
+                    results.add(result)
+                }
+                results
             }
         } catch (e: Exception) {
```
- **验证清单**:
  1. 同时传入2+张不同桌的截图，检查每张的识别结果是否独立正确
  2. 检查 `holeCardsLocked` 在多桌调用后是否未被交叉污染
  3. 验证串行化后性能是否可接受（每张约2-3秒，2桌=4-6秒）
- **置信度**: 高

---

### P2-NEW-3: ScreenOptService.onDestroy未清除onScreenshotReady回调
- **优先级**: P2（中等）
- **文件**: ScreenOptService.kt 第64-67行
- **业务意图**: 无障碍服务销毁时清理资源
- **根因推理链**:
  1. `onScreenshotReady` 是 `ScreenOptService` companion object 的静态变量
  2. FloatingService 在截屏流程中设置：`ScreenOptService.onScreenshotReady = { s -> handler.post { ... } }`
  3. 这个 lambda 闭包捕获了 FloatingService 实例（`handler`、`this` 等引用）
  4. `ScreenOptService.onDestroy()` 只清了 `instance = null` 和 `isRunning = false`，**没有清 `onScreenshotReady`**
  5. 如果系统独立杀死 ScreenOptService（用户关闭无障碍权限）但 FloatingService 还活着，`onScreenshotReady` 仍持有旧的 lambda 引用
  6. 更严重：如果 FloatingService 先被销毁→设 `onScreenshotReady = null`→但 ScreenOptService 重建→captureScreen() 触发 `onScreenshotReady?.invoke(false)`→null-safe 不调用，但此时 FloatingService 的 autoCapture 流程永远等不到回调→卡死
- **全局影响面**: 服务生命周期不同步时，自动截屏流程卡死（isVisionInProgress永远为true），或持有已销毁Service的引用导致内存泄漏。
- **精简Diff**:
```diff
--- a/app/src/main/java/com/pokerhelper/app/ScreenOptService.kt
+++ b/app/src/main/java/com/pokerhelper/app/ScreenOptService.kt
@@ -63,6 +63,7 @@ class ScreenOptService : AccessibilityService() {
     override fun onDestroy() {
         super.onDestroy()
+        onScreenshotReady = null  // P2-fix: 清除回调防止泄漏和悬空引用
         instance = null
         isRunning = false
     }
```
- **验证清单**:
  1. 关闭无障碍服务后检查 ScreenOptService.onScreenshotReady 是否为 null
  2. 在 FloatingService 运行时独立重启无障碍服务，验证截屏恢复
  3. 使用 LeakCanary 检查 FloatingService 是否泄漏
- **置信度**: 高

---

### P2-NEW-4: ChipTracker每次OCR调用创建TextRecognition客户端不关闭
- **优先级**: P2（中等）
- **文件**: ChipTracker.kt 第266、288、314行
- **业务意图**: 定点OCR读取筹码/文本信息
- **根因推理链**:
  1. `ocrRegion()` (L266)、`ocrRegionText()` (L288)、`analyzeScreenshot()` (L314) 每次都 `TextRecognition.getClient(...)` 创建新实例
  2. 创建后 **从未调用 `recognizer.close()`**（grep确认无 close 调用）
  3. 对比：CardRecognizer.readPotSize() 中正确调用了 `recognizer.close()`
  4. ML Kit 的 TextRecognizer 内部持有线程池和模型资源，不关闭导致资源持续累积
  5. `analyzeWithFixedCoords` 对6个座位+底池各调用一次，每次截屏至少创建 8 个 TextRecognition 实例
  6. 高频自动截屏（每2-4秒）= 每分钟创建 15-30 个未关闭的 recognizer
- **全局影响面**: 长时间运行后内存持续增长（每个recognizer持有模型引用），GC无法回收，最终OOM崩溃。
- **精简Diff**:
```diff
--- a/app/src/main/java/com/pokerhelper/app/ChipTracker.kt
+++ b/app/src/main/java/com/pokerhelper/app/ChipTracker.kt
@@ -262,6 +262,7 @@ object ChipTracker {
     /** 定点OCR：返回数字金额 */
     private fun ocrRegion(cropBitmap: Bitmap): Long? {
         val latch = CountDownLatch(1)
         var resultText = ""
+        val recognizer = TextRecognition.getClient(TextRecognizerOptions.DEFAULT_OPTIONS)
 
         val image = InputImage.fromBitmap(cropBitmap, 0)
-        val recognizer = TextRecognition.getClient(TextRecognizerOptions.DEFAULT_OPTIONS)
-
         recognizer.process(image)
             .addOnSuccessListener { visionText ->
                 resultText = visionText.text
                 latch.countDown()
             }
             .addOnFailureListener { e ->
                 Log.w(TAG, "区域OCR失败: ${e.message}")
                 latch.countDown()
             }
 
-        latch.await(2, TimeUnit.SECONDS)
+        try {
+            latch.await(2, TimeUnit.SECONDS)
+        } finally {
+            recognizer.close()
+        }
         return parseChipText(resultText)
     }
 
     /** 定点OCR：返回原始文本 */
     private fun ocrRegionText(cropBitmap: Bitmap): String {
         val latch = CountDownLatch(1)
         var resultText = ""
+        val recognizer = TextRecognition.getClient(TextRecognizerOptions.DEFAULT_OPTIONS)
 
         val image = InputImage.fromBitmap(cropBitmap, 0)
-        val recognizer = TextRecognition.getClient(TextRecognizerOptions.DEFAULT_OPTIONS)
-
         recognizer.process(image)
             .addOnSuccessListener { visionText ->
                 resultText = visionText.text.trim()
                 latch.countDown()
             }
             .addOnFailureListener { e ->
                 Log.w(TAG, "区域文本OCR失败: ${e.message}")
                 latch.countDown()
             }
 
-        latch.await(2, TimeUnit.SECONDS)
+        try {
+            latch.await(2, TimeUnit.SECONDS)
+        } finally {
+            recognizer.close()
+        }
         return resultText
     }
```
  （`analyzeScreenshot` L314同理）
- **验证清单**:
  1. 运行10分钟自动截屏，对比修复前后的内存占用（adb shell dumpsys meminfo）
  2. 用 Android Profiler 检查 TextRecognition 实例数是否持续增长
- **置信度**: 高

---

### P2-NEW-5: pendingJsCalls跨线程访问无同步保护
- **优先级**: P2（中等）
- **文件**: FloatingService.kt 第161、745-746、1453-1454行
- **业务意图**: WebView未就绪时暂存JS调用，就绪后批量执行
- **根因推理链**:
  1. `pendingJsCalls = mutableListOf<String>()` 是普通 ArrayList，非线程安全
  2. 主线程写入：`executeJs()` (L745-746) 在主线程调用 `pendingJsCalls.add(js)`
  3. WebView线程读取：`onPageFinished` (L1453-1454) 在 WebView 内部线程执行 `ArrayList(pendingJsCalls)` + `pendingJsCalls.clear()`
  4. `onPageFinished` 的调用线程是 WebView 的 JavaBridge 线程（非主线程），由 WebView 内部调度
  5. 如果 `executeJs` 在 `onPageFinished` 遍历 `pendingJsCalls` 时同时 `add`，触发 `ConcurrentModificationException`
  6. 虽然 `onPageFinished` 仅在 WebView 加载完成时触发一次，概率较低，但在服务重启/re-init 场景下可能多次触发
- **全局影响面**: 低频但致命——崩溃导致 FloatingService 的 WebView 策略引擎无法初始化，所有后续识别结果无法送达 JS 引擎。
- **精简Diff**:
```diff
--- a/app/src/main/java/com/pokerhelper/app/FloatingService.kt
+++ b/app/src/main/java/com/pokerhelper/app/FloatingService.kt
@@ -161,7 +161,8 @@ class FloatingService : Service() {
     // V2.9.4: WebView加载追踪 + JS调用队列
     private var webViewReady = false
     private var _webViewRetryCount = 0
     private val _WEBVIEW_MAX_RETRY = 3
     private val _PENDING_JS_MAX = 50
-    private val pendingJsCalls = mutableListOf<String>()
+    // P2-fix: 线程安全的JS队列，防止WebView线程与主线程并发修改
+    private val pendingJsCalls = java.util.Collections.synchronizedList(mutableListOf<String>())
```
- **验证清单**:
  1. 在 WebView 加载过程中连续触发 executeJs，检查是否崩溃
  2. 服务重启场景验证 JS 队列不丢不重
- **置信度**: 中（触发概率低但后果严重）

---

### P2-NEW-6: DiagnosticLogger筹码追踪字段多线程无同步
- **优先级**: P2（中等）
- **文件**: DiagnosticLogger.kt 第539-550行
- **业务意图**: 计算前后帧筹码/底池变化量，用于诊断分析
- **根因推理链**:
  1. `lastChips` 和 `lastPot` 是 `DiagnosticLogger`（object单例）的普通 var 字段
  2. `calcChipDelta` / `calcPotDelta` 是读-改-写复合操作：`val delta = if(lastChips > 0) ... ; lastChips = currentChips`
  3. `logRecognition` 可从主线程（本地CV路径）和后台线程（API路径、后台HUD补充路径）调用
  4. 两个线程同时调用 `logRecognition` 时，线程A读到 `lastChips=1000`，线程B也读到 `lastChips=1000`，然后都写入 `lastChips=当前值`，丢失一次delta
  5. 虽然只影响诊断数据（不影响核心功能），但会导致导出的复盘日志中筹码变化数据不准确
- **全局影响面**: 复盘导出数据失真，影响用户分析自己的输赢走势。
- **精简Diff**:
```diff
--- a/app/src/main/java/com/pokerhelper/app/DiagnosticLogger.kt
+++ b/app/src/main/java/com/pokerhelper/app/DiagnosticLogger.kt
@@ -539,13 +539,15 @@ object DiagnosticLogger {
-    private var lastChips = 0
-    private var lastPot = 0
-    
-    private fun calcChipDelta(currentChips: Int): Long {
-        val delta = if (lastChips > 0) (currentChips - lastChips).toLong() else null
-        lastChips = currentChips
-        return delta ?: 0L
-    }
-    
-    private fun calcPotDelta(currentPot: Int): Int {
-        val delta = if (lastPot > 0) currentPot - lastPot else 0
-        lastPot = currentPot
-        return delta
+    @Volatile private var lastChips = 0
+    @Volatile private var lastPot = 0
+    private val chipTrackLock = Any()
+
+    private fun calcChipDelta(currentChips: Int): Long {
+        synchronized(chipTrackLock) {
+            val delta = if (lastChips > 0) (currentChips - lastChips).toLong() else null
+            lastChips = currentChips
+            return delta ?: 0L
+        }
+    }
+
+    private fun calcPotDelta(currentPot: Int): Int {
+        synchronized(chipTrackLock) {
+            val delta = if (lastPot > 0) currentPot - lastPot else 0
+            lastPot = currentPot
+            return delta
+        }
     }
```
- **验证清单**:
  1. 导出复盘日志，检查筹码变化数据是否连续合理
  2. 多线程压力测试：并发调用 logRecognition 检查数据一致性
- **置信度**: 中

---

### P3-NEW-7: HttpServerService /api/capture覆写共享回调
- **优先级**: P3（轻微）
- **文件**: HttpServerService.kt 第260-276行
- **业务意图**: HTTP API端点提供按需截屏功能
- **根因推理链**:
  1. `/api/capture` 处理逻辑设置 `ScreenOptService.onScreenshotReady = { ... }` 然后 `latch.await(3s)`
  2. 同时 FloatingService 的 `triggerCapture()` / `autoCaptureTrigger()` 也会设置同一个 `onScreenshotReady`
  3. 如果外部HTTP客户端（如开发调试工具）和 FloatingService 同时请求截屏，后设置的回调会覆盖前者
  4. 被覆盖的回调永远不会被调用，对应的 `latch.await` 直到超时
- **全局影响面**: 仅影响调试场景下的 HTTP API 截屏功能，正常使用中不太可能触发。
- **精简Diff**:
```diff
--- a/app/src/main/java/com/pokerhelper/app/HttpServerService.kt
+++ b/app/src/main/java/com/pokerhelper/app/HttpServerService.kt
@@ -257,6 +257,10 @@ class HttpServerService : Service() {
                         // V2.1: 按需截屏+API识别
                         session.uri == "/api/capture" -> {
                             try {
+                                // P3-fix: 检查是否有正在进行的截屏流程
+                                if (ScreenOptService.onScreenshotReady != null) {
+                                    val json = JSONObject().apply { put("ok", false); put("error", "capture_in_progress") }.toString()
+                                    return@serve newFixedLengthResponse(Response.Status.CONFLICT, "application/json", json)
+                                }
                                 if (ScreenOptService.isServiceRunning()) {
                                     val latch = java.util.concurrent.CountDownLatch(1)
```
- **验证清单**:
  1. 并发调用 /api/capture 和手动截屏按钮，验证两者互不干扰
- **置信度**: 中

---

### P3-NEW-8: LocalSceneRecognizer持有Activity/Service Context
- **优先级**: P3（轻微）
- **文件**: LocalSceneRecognizer.kt 第27行，FloatingService.kt `reinitializeComponents()`
- **业务意图**: 本地场景识别器需要Context来加载资源
- **根因推理链**:
  1. `LocalSceneRecognizer` 构造参数是 `context: Context`，存为 `private val context`
  2. FloatingService 创建时传入 `this`（Service Context）：`sceneRecognizer = LocalSceneRecognizer(this, cardRecognizer!!)`
  3. `reinitializeComponents()` 也传入 `this`
  4. 虽然 Service 的 lifecycle 和 LocalSceneRecognizer 一致（都在 onDestroy 中释放），但传入 `applicationContext` 更安全
  5. 对比：`VisionApiClient.initLocalRecognizer` 正确使用了 `context.applicationContext`
- **全局影响面**: 极低风险。Service Context 的生命周期与 LocalSceneRecognizer 一致，但若 Service 被 recreate（如配置变更），旧 Context 被持有的时间可能超出预期。
- **精简Diff**:
```diff
--- a/app/src/main/java/com/pokerhelper/app/FloatingService.kt
+++ b/app/src/main/java/com/pokerhelper/app/FloatingService.kt
@@ -292,7 +292,7 @@ class FloatingService : Service() {
             cardRecognizer = CardRecognizer(this)
             cardRecognizer?.init()
             CardRecognizer.updateScreenSize(resources.displayMetrics.widthPixels, resources.displayMetrics.heightPixels)
-            sceneRecognizer = LocalSceneRecognizer(this, cardRecognizer!!)
+            sceneRecognizer = LocalSceneRecognizer(applicationContext, cardRecognizer!!)
             sceneRecognizer!!.init(resources.displayMetrics.widthPixels, resources.displayMetrics.heightPixels)
```
  （`onCreate` 和 `reinitializeComponents` 中的 `CardRecognizer(this)` 同理改为 `CardRecognizer(applicationContext)`）
- **验证清单**:
  1. 使用 LeakCanary 检测 Service recreate 后是否有 Context 泄漏
- **置信度**: 低

---

## 总结

| 编号 | 优先级 | 文件 | 问题 | 置信度 |
|------|--------|------|------|--------|
| P1-NEW-1 | **P1** | FloatingService.kt:1003,1013,1030 | executeExactBet主线程Thread.sleep~1秒 | 高 |
| P1-NEW-2 | **P1** | VisionApiClient.kt:1060-1068 | 多桌并行分析并发修改单例共享状态 | 高 |
| P2-NEW-3 | **P2** | ScreenOptService.kt:64-67 | onDestroy未清onScreenshotReady回调 | 高 |
| P2-NEW-4 | **P2** | ChipTracker.kt:266,288,314 | TextRecognition客户端创建不关闭 | 高 |
| P2-NEW-5 | **P2** | FloatingService.kt:161,1453 | pendingJsCalls跨线程无同步 | 中 |
| P2-NEW-6 | **P2** | DiagnosticLogger.kt:539-550 | 筹码追踪字段多线程无同步 | 中 |
| P3-NEW-7 | **P3** | HttpServerService.kt:260 | /api/capture覆写共享回调 | 中 |
| P3-NEW-8 | **P3** | FloatingService.kt:292 | LocalSceneRecognizer持有Service Context | 低 |

**本轮发现 8 个新Bug**：2个P1 + 4个P2 + 2个P3

### 最高优先级修复建议
1. **P1-NEW-1**（executeExactBet ANR）：将 Thread.sleep 逻辑移到后台线程，或使用 Handler.postDelayed 链式调度
2. **P1-NEW-2**（多表并行数据竞争）：将 `analyzeMultipleScreenshots` 改为串行执行，或为每次调用创建独立的分析上下文
