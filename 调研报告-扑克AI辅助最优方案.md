# 扑克AI辅助 最优方案调研报告

## 一、关键发现：别人怎么做的

### 1. RTA Poker（rta.poker）— 最专业
- **预计算GTO数据库**：1500+TB，370万+预解算，**不需要实时模拟**
- OCR 200ms内完成识别
- 现代化Overlay直接显示在屏幕上
- 支持4K分辨率，12-16桌同时

**→ 核心启示：不要实时跑蒙特卡洛，用预计算数据库**

### 2. PokerGPT（pokergpt.org）— 最贴近我们
- 屏幕截图 → 实时图像解析
- **画中画(PiP)权限** → 系统级置顶小窗口（比悬浮窗更紧凑）
- 神经网络预训练模型（非实时模拟）
- 响应时间 2-3秒

**→ 核心启示：用画中画替代悬浮窗，用预训练模型替代实时模拟**

### 3. Google Play Games Sidekick（2025.9发布）
- 可移动标签设计，点击展开面板
- Gemini AI通过屏幕共享实时分析游戏

**→ 核心启示：最小化默认状态 + 点击展开 = 最佳手机体验**

---

## 二、我们当前问题 vs 行业解法

| 问题 | 我们当前 | 行业最优 | 差距 |
|------|---------|---------|------|
| 策略计算 | JS同步mcVsRange(2000) | 预计算数据库/Web Worker+WASM | 巨大 |
| UI显示 | 全屏悬浮窗+绿色顶栏 | 画中画/最小化标签 | 大 |
| 识别延迟 | 2-5秒（API视觉） | OCR 200ms | 大 |
| 建议渲染 | 同步阻塞UI | 渐进式：先估后精 | 中 |

---

## 三、推荐实施方案（按优先级）

### P0（已实施）：go()异步化 + try/catch兜底
- ✅ setTimeout让出JS线程
- ✅ "🧠思考中..."提示
- ✅ showAdvice移入异步回调
- ✅ getBlockerScore修s→SS

### P1（下一步建议）：Web Worker隔离蒙特卡洛
**为什么**：setTimeout(50ms)只是权宜之计，mcVsRange(2000)仍然在主线程跑，可能造成卡顿

**怎么做**：
1. 把mcVsRange逻辑提取到独立JS文件 `mc-worker.js`
2. 主线程通过postMessage发送模拟参数
3. Worker计算完返回{eq, win, tie}
4. 主线程接收结果后渲染建议

**预期效果**：彻底不阻塞UI，甚至可以把iterations提到10000+

### P2（中期）：预计算GTO数据库
**为什么**：RTA Poker的1500TB方案太重，但我们可以做一个轻量版

**怎么做**：
1. 离线预计算常见场景（翻前169手牌×6位置×3桌型≈3000条）
2. 存为JSON内嵌到App中
3. 翻前直接查表，翻后用Worker计算
4. 翻前响应从秒级降到毫秒级

### P3（UI优化）：最小化标签 + 画中画
**为什么**：豪哥反馈绿色背景占空间，游戏中需要最紧凑

**怎么做**：
1. 默认状态：屏幕边缘小标签（如"AA 72%"），可拖动
2. 点击展开：半透明面板显示详细建议
3. 或者用Android画中画API（更原生、更稳定）

---

## 四、V2.9.14已做的优化

1. **顶栏**：绿色→半透明深色，padding缩小
2. **res精简**：极速模式隐藏DRTA/SPR/EV/规划/剥削等次要信息
3. **r-tip**：48px→20px，从巨大提示变成紧凑标签
4. **核心修复**：getBlockerScore s→SS，解决"s is not defined"
5. **showAdvice异步**：移入go()内部回调，游戏中折叠态也能看到建议

---

## 五、GitHub Token提醒

⚠️ Token将于2026-06-09到期，还剩约1天，到期后无法构建APK！
